import { StyleSheet } from 'react-native';
import { Colors } from '../../utils';

const styles = StyleSheet.create({
    text: {
        color: Colors.secondaryButton.color,
        fontFamily: 'Montserrat-Bold',
        fontSize: 12,
        lineHeight: 15,
        letterSpacing: 0.24,
        textAlign: 'center'
    }
});

export default styles;